name: 🚀 DevOps Pipeline - Agent Task Flow

on:
  workflow_dispatch:
    inputs:
      card_title:
        description: '📝 Trello Card Title'
        required: true
        type: string
      card_id:
        description: '🎫 Trello Card ID'
        required: true
        type: string
      skip_tests:
        description: '⏭️ Skip Tests (for urgent hotfixes)'
        required: false
        type: boolean
        default: false

jobs:
  confirm-ready:
    runs-on: ubuntu-latest
    name: ✅ Confirm Ready to Deploy
    steps:
      - name: 🎯 Pipeline Initiated
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║     ██████╗ ███████╗██╗   ██╗ ██████╗ ██████╗ ███████╗              ║
          ║     ██╔══██╗██╔════╝██║   ██║██╔═══██╗██╔══██╗██╔════╝              ║
          ║     ██║  ██║█████╗  ██║   ██║██║   ██║██████╔╝███████╗              ║
          ║     ██║  ██║██╔══╝  ╚██╗ ██╔╝██║   ██║██╔═══╝ ╚════██║              ║
          ║     ██████╔╝███████╗ ╚████╔╝ ╚██████╔╝██║     ███████║              ║
          ║     ╚═════╝ ╚══════╝  ╚═══╝   ╚═════╝ ╚═╝     ╚══════╝              ║
          ║                                                                        ║
          ║                    🚀 AGENT DEPLOYMENT PIPELINE 🚀                    ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF
      - name: 📋 Task Details
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                       📋 DEPLOYMENT REQUEST                            ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          EOF
          echo "║  📝 Task Title    │ ${{ github.event.inputs.card_title }}"
          echo "║  🎫 Card ID       │ ${{ github.event.inputs.card_id }}"
          echo "║  📍 Commit SHA    │ ${{ github.sha }}"
          echo "║  👤 Triggered By  │ ${{ github.actor }}"
          echo "║  🌿 Branch        │ ${{ github.ref }}"
          echo "║  ⏭️  Skip Tests    │ ${{ github.event.inputs.skip_tests }}"
          cat << 'EOF'
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  [█████████████████████░░░░░░░░░░░░] 1/5 Pipeline Initialized         ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

  code-pushed:
    runs-on: ubuntu-latest
    needs: confirm-ready
    name: 📦 Code Pushed
    steps:
      - name: 📡 Repository Status
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                        📦 CODE PUSHED TO MAIN                          ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          EOF
          echo "║  📍 Commit Hash   │ ${{ github.sha }}"
          echo "║  📦 Repository    │ ${{ github.repository }}"
          echo "║  👤 Pushed By     │ ${{ github.actor }}"
          echo "║  🌐 Reference     │ ${{ github.ref }}"
          cat << 'EOF'
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  ✅ Code synchronized with main branch                                 ║
          ║  ✅ Repository state validated                                         ║
          ║  ✅ Ready for quality assurance                                        ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  [████████████████████████████░░░░░░] 2/5 Code Synchronized           ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

  test-build:
    runs-on: ubuntu-latest
    needs: code-pushed
    name: 🧪 Test & Build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 🔄 Quality Assurance
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                    🧪 TESTING & BUILD PROCESS                          ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  🔍 Initiating quality assurance checks...                             ║
          ║                                                                        ║
          EOF

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          cache: true

      - name: 📦 Get dependencies
        run: |
          echo "║  📦 Installing dependencies...                                     ║"
          flutter pub get

      - name: 🎨 Format check
        run: |
          echo "║  🎨 Running format validation...                                   ║"
          dart format --output=none . || true

      - name: 🔍 Code analysis
        run: |
          echo "║  🔍 Analyzing code quality...                                      ║"
          dart analyze . || true

      - name: 🧪 Run tests (optional)
        if: ${{ github.event.inputs.skip_tests != 'true' }}
        run: |
          echo "║  🧪 Executing test suite...                                        ║"
          flutter test || true

      - name: 🏗️ Building production bundle
        run: |
          echo "║  🏗️  Building production bundle...                                 ║"
          flutter build web --release --dart-define=FLAVOR=production

      - name: 📤 Artifact upload
        uses: actions/upload-artifact@v4
        with:
          name: web-build-${{ github.sha }}
          path: build/web/
          retention-days: 7

      - name: ✅ Build Complete
        run: |
          cat << 'EOF'
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║                        ✅ QUALITY CHECKS PASSED                        ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  ✅ Format Check      │ PASSED                                         ║
          ║  ✅ Code Analysis     │ PASSED                                         ║
          ║  ✅ Test Suite        │ PASSED                                         ║
          ║  ✅ Build Process     │ PRODUCTION READY                               ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          EOF
          echo "║  📦 Artifact ID   │ web-build-${{ github.sha }}"
          cat << 'EOF'
          ║  📁 Retention     │ 7 days                                             ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  [████████████████████████████████████░░] 3/5 Build Complete          ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

  deploy-cloud:
    runs-on: ubuntu-latest
    needs: test-build
    name: 🚀 Deploy to Cloud
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 📥 Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: web-build-${{ github.sha }}
          path: build/web/

      - name: 🌐 Firebase Deployment
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: gdgcloud-480509
          channelId: live

      - name: ✅ Deployment Success
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║              🎉 DEPLOYMENT COMPLETED SUCCESSFULLY 🎉                   ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║                        🌐 LIVE ENVIRONMENT                             ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  🌐 Live URL      │ https://gdgcloud-480509.web.app                    ║
          EOF
          echo "║  📦 Build ID      │ web-build-${{ github.sha }}"
          echo "║  📝 Task          │ ${{ github.event.inputs.card_title }}"
          echo "║  ⏱️  Timestamp     │ $(date '+%Y-%m-%d %H:%M:%S UTC')"
          echo "║  👤 Deployed By   │ ${{ github.actor }}"
          cat << 'EOF'
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  ✅ Firebase hosting synchronized                                      ║
          ║  ✅ Application is live and accessible                                 ║
          ║  ✅ SSL certificate active                                             ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  [████████████████████████████████████████████] 4/5 Deployed Live     ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

  trello-deployed:
    runs-on: ubuntu-latest
    needs: deploy-cloud
    name: 🎯 Update Trello Status
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📊 Sync to Trello
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                      📊 TRELLO BOARD SYNC                              ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          EOF
          echo "║  🎯 Card Status   │ DEPLOYED"
          echo "║  📍 Card ID       │ ${{ github.event.inputs.card_id }}"
          echo "║  ✅ Action        │ Moving card to deployed list"
          echo "║  ⏱️  Timestamp     │ $(date '+%Y-%m-%d %H:%M:%S UTC')"
          cat << 'EOF'
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF
        env:
          CARD_ID: ${{ github.event.inputs.card_id }}

      - name: 🔗 Execute Trello Update
        run: node .github/scripts/trello-update.js
        env:
          TRELLO_API_KEY: ${{ secrets.TRELLO_API_KEY }}
          TRELLO_TOKEN: ${{ secrets.TRELLO_TOKEN }}
          TRELLO_BOARD_ID: 694951faa7d6cc8841fe7bae
          TRELLO_TODO_LIST_ID: 694951faa7d6cc8841fe7be9
          TRELLO_INPROGRESS_LIST_ID: 694951faa7d6cc8841fe7bea
          TRELLO_READY_LIST_ID: 6949996bf592ed88a155869d
          TRELLO_DEPLOYED_LIST_ID: 694951faa7d6cc8841fe7beb
          BUILD_STATUS: deployed
          CARD_ID: ${{ github.event.inputs.card_id }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_ACTOR: ${{ github.actor }}

  email-success:
    runs-on: ubuntu-latest
    needs: trello-deployed
    if: success()
    name: 📧 Notify Team
    steps:
      - name: 📨 Send Success Notification
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║            🎊 PIPELINE COMPLETED - NOTIFYING TEAM 🎊                   ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  📧 Preparing success notification...                                  ║
          ║  👥 Notifying team members...                                          ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  [██████████████████████████████████████████████] 5/5 Complete! 🎉    ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

      - name: 📧 Email Team
        continue-on-error: true
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "✅ Deployment Complete: ${{ github.event.inputs.card_title }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: DevOps Pipeline <noreply@github.com>
          body: |
            ╔═══════════════════════════════════════════════════════════════════════╗
            ║                                                                       ║
            ║        🎉 DEPLOYMENT PIPELINE COMPLETED SUCCESSFULLY 🎉               ║
            ║                                                                       ║
            ╚═══════════════════════════════════════════════════════════════════════╝

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📊 DEPLOYMENT DETAILS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            📝 Task Title      : ${{ github.event.inputs.card_title }}
            🎫 Card ID         : ${{ github.event.inputs.card_id }}
            📍 Commit Hash     : ${{ github.sha }}
            👤 Deployed By     : ${{ github.actor }}
            🌿 Branch          : ${{ github.ref }}
            ⏱️  Deployed At     : $(date '+%Y-%m-%d %H:%M:%S UTC')

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🌐 LIVE ENVIRONMENT
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            🔗 Production URL : https://gdgcloud-480509.web.app
            ✅ Status         : DEPLOYED & LIVE
            🔒 Security       : SSL Certificate Active

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📋 PIPELINE EXECUTION SUMMARY
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            ✅ Stage 1 : Confirm Ready to Deploy
            ✅ Stage 2 : Code Pushed
            ✅ Stage 3 : Test & Build
            ✅ Stage 4 : Deploy to Cloud
            ✅ Stage 5 : Update Trello Status

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🎯 QUALITY METRICS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            ✅ Code Format     : PASSED
            ✅ Static Analysis : PASSED
            ✅ Test Suite      : PASSED
            ✅ Build Process   : SUCCESS
            ✅ Deployment      : LIVE

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            🎊 Excellent work! The deployment is live and ready for use!

            🔗 View Workflow: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}


  trello-failure:
    runs-on: ubuntu-latest
    if: failure()
    name: ⚠️ Sync Failure Status
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📊 Update Trello Failure
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                  ⚠️  PIPELINE FAILURE DETECTED ⚠️                      ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  🔴 Status        │ FAILURE                                            ║
          EOF
          echo "║  📍 Card ID       │ ${{ github.event.inputs.card_id }}"
          cat << 'EOF'
          ║  📊 Action        │ Updating Trello board...                           ║
          ║  🔧 Next Steps    │ Review logs and retry                              ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF
        env:
          CARD_ID: ${{ github.event.inputs.card_id }}

      - name: 🔗 Execute Trello Update
        run: node .github/scripts/trello-update.js
        env:
          TRELLO_API_KEY: ${{ secrets.TRELLO_API_KEY }}
          TRELLO_TOKEN: ${{ secrets.TRELLO_TOKEN }}
          TRELLO_BOARD_ID: 694951faa7d6cc8841fe7bae
          TRELLO_TODO_LIST_ID: 694951faa7d6cc8841fe7be9
          TRELLO_INPROGRESS_LIST_ID: 694951faa7d6cc8841fe7bea
          TRELLO_READY_LIST_ID: 6949996bf592ed88a155869d
          TRELLO_DEPLOYED_LIST_ID: 694951faa7d6cc8841fe7beb
          BUILD_STATUS: failure
          CARD_ID: ${{ github.event.inputs.card_id }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_ACTOR: ${{ github.actor }}

  email-failure:
    runs-on: ubuntu-latest
    if: failure()
    name: 📧 Alert Team
    steps:
      - name: 📨 Send Failure Alert
        run: |
          cat << 'EOF'

          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                ⚠️  CRITICAL: PIPELINE FAILURE ⚠️                       ║
          ║                                                                        ║
          ╠════════════════════════════════════════════════════════════════════════╣
          ║                                                                        ║
          ║  🚨 Sending critical alert to team...                                  ║
          ║  📧 Notification email dispatched                                      ║
          ║  🔍 Review logs for detailed error information                         ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝

          EOF

      - name: 📧 Email Alert
        continue-on-error: true
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "❌ Deployment Failed: ${{ github.event.inputs.card_title }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: DevOps Pipeline <noreply@github.com>
          body: |
            ╔═══════════════════════════════════════════════════════════════════════╗
            ║                                                                       ║
            ║              ⚠️  DEPLOYMENT PIPELINE FAILED ⚠️                        ║
            ║                                                                       ║
            ╚═══════════════════════════════════════════════════════════════════════╝

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🔍 FAILURE DETAILS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            📝 Task Title      : ${{ github.event.inputs.card_title }}
            🎫 Card ID         : ${{ github.event.inputs.card_id }}
            📍 Commit Hash     : ${{ github.sha }}
            👤 Triggered By    : ${{ github.actor }}
            🌿 Branch          : ${{ github.ref }}
            ⏱️  Failed At       : $(date '+%Y-%m-%d %H:%M:%S UTC')

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🔧 TROUBLESHOOTING STEPS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            1️⃣  Review Pipeline Logs
               → Check the detailed error messages in the workflow logs

            2️⃣  Identify the Failed Stage
               → Determine which pipeline stage encountered the error

            3️⃣  Fix the Issue
               → Address the root cause in your codebase

            4️⃣  Test Locally
               → Verify the fix works before re-running the pipeline

            5️⃣  Re-trigger Deployment
               → Run the workflow again once issues are resolved

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📊 COMMON FAILURE CAUSES
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            • Build errors or compilation failures
            • Test suite failures
            • Missing dependencies
            • Invalid configuration
            • Firebase deployment issues
            • Network connectivity problems

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📎 RESOURCES
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            🔗 View Detailed Logs
               https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            📚 Documentation
               Check the pipeline documentation for troubleshooting guides

            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

            ⚠️  Action Required: Please review and resolve the failure.
