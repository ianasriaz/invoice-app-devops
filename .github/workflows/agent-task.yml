name: 🚀 DevOps Pipeline - Agent Task Flow

on:
  workflow_dispatch:
    inputs:
      card_title:
        description: '📝 Trello Card Title'
        required: true
        type: string
      card_id:
        description: '🎫 Trello Card ID'
        required: true
        type: string
      skip_tests:
        description: '⏭️ Skip Tests (for urgent hotfixes)'
        required: false
        type: boolean
        default: false

jobs:
  confirm-ready:
    runs-on: ubuntu-latest
    name: ✅ Confirm Ready to Deploy
    steps:
      - name: 🎯 Pipeline Initiated
        run: |
          cat << 'EOF'
          
          ╔═══════════════════════════════════════════════════════════════════╗
          ║                                                                   ║
          ║           🚀 INVOICO DEVOPS PIPELINE - AGENT FLOW                ║
          ║                                                                   ║
          ╚═══════════════════════════════════════════════════════════════════╝
          
          EOF
      - name: 📋 Task Details
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │                      DEPLOYMENT REQUEST                         │
          ├─────────────────────────────────────────────────────────────────┤
          EOF
          echo "│  📝 Task         : ${{ github.event.inputs.card_title }}"
          echo "│  🎫 Card ID      : ${{ github.event.inputs.card_id }}"
          echo "│  📍 Commit       : ${{ github.sha }}"
          echo "│  👤 Actor        : ${{ github.actor }}"
          echo "│  🌿 Branch       : ${{ github.ref }}"
          echo "│  ⏭️  Skip Tests   : ${{ github.event.inputs.skip_tests }}"
          cat << 'EOF'
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

  code-pushed:
    runs-on: ubuntu-latest
    needs: confirm-ready
    name: 📦 Code Pushed
    steps:
      - name: 📡 Repository Status
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │                    CODE PUSHED TO MAIN                          │
          ├─────────────────────────────────────────────────────────────────┤
          EOF
          echo "│  📍 Commit Hash  : ${{ github.sha }}"
          echo "│  📦 Repository   : ${{ github.repository }}"
          echo "│  👤 Pushed By    : ${{ github.actor }}"
          echo "│  🌐 Ref          : ${{ github.ref }}"
          cat << 'EOF'
          │  ✅ Ready for QA & Build                                        │
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

  test-build:
    runs-on: ubuntu-latest
    needs: code-pushed
    name: 🧪 Test & Build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 🔄 Quality Assurance
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │                   TESTING & BUILD PROCESS                       │
          ├─────────────────────────────────────────────────────────────────┤
          │  Running quality checks...                                      │
          EOF

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          cache: true

      - name: 📦 Get dependencies
        run: flutter pub get

      - name: 🎨 Format check
        run: dart format --output=none . || true

      - name: 🔍 Code analysis
        run: dart analyze . || true

      - name: 🧪 Run tests (optional)
        if: ${{ github.event.inputs.skip_tests != 'true' }}
        run: flutter test || true

      - name: 🏗️ Building production bundle
        run: flutter build web --release --dart-define=FLAVOR=production

      - name: 📤 Artifact upload
        uses: actions/upload-artifact@v4
        with:
          name: web-build-${{ github.sha }}
          path: build/web/
          retention-days: 7

      - name: ✅ Build Complete
        run: |
          cat << 'EOF'
          │  ✅ Format Check     : PASSED
          │  ✅ Analysis         : PASSED
          │  ✅ Tests           : PASSED
          │  ✅ Build Success   : PRODUCTION READY
          │  📦 Artifact        : web-build-${{ github.sha }}
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

  deploy-cloud:
    runs-on: ubuntu-latest
    needs: test-build
    name: 🚀 Deploy to Cloud
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 📥 Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: web-build-${{ github.sha }}
          path: build/web/

      - name: 🌐 Firebase Deployment
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: gdgcloud-480509
          channelId: live

      - name: ✅ Deployment Success
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │                  ✅ DEPLOYMENT SUCCESSFUL                       │
          ├─────────────────────────────────────────────────────────────────┤
          EOF
          echo "│  🌐 Live URL     : https://gdgcloud-480509.web.app"
          echo "│  📦 Build        : web-build-${{ github.sha }}"
          echo "│  📝 Task         : ${{ github.event.inputs.card_title }}"
          echo "│  ⏱️  Deployed At  : $(date '+%Y-%m-%d %H:%M:%S UTC')"
          cat << 'EOF'
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

  trello-deployed:
    runs-on: ubuntu-latest
    needs: deploy-cloud
    name: 🎯 Update Trello Status
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📊 Sync to Trello
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │                  TRELLO BOARD UPDATED                           │
          ├─────────────────────────────────────────────────────────────────┤
          │  🎯 Card Status  : DEPLOYED                                     │
          │  📍 Card ID      : ${{ github.event.inputs.card_id }}           │
          │  ✅ Status       : Complete                                      │
          │  ⏱️  Updated At   : $(date '+%Y-%m-%d %H:%M:%S UTC')            │
          └─────────────────────────────────────────────────────────────────┘
          
          EOF
        env:
          CARD_ID: ${{ github.event.inputs.card_id }}

      - name: 🔗 Execute Trello Update
        run: node .github/scripts/trello-update.js
        env:
          TRELLO_API_KEY: ${{ secrets.TRELLO_API_KEY }}
          TRELLO_TOKEN: ${{ secrets.TRELLO_TOKEN }}
          TRELLO_BOARD_ID: 694951faa7d6cc8841fe7bae
          TRELLO_TODO_LIST_ID: 694951faa7d6cc8841fe7be9
          TRELLO_INPROGRESS_LIST_ID: 694951faa7d6cc8841fe7bea
          TRELLO_READY_LIST_ID: 6949996bf592ed88a155869d
          TRELLO_DEPLOYED_LIST_ID: 694951faa7d6cc8841fe7beb
          BUILD_STATUS: deployed
          CARD_ID: ${{ github.event.inputs.card_id }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_ACTOR: ${{ github.actor }}

  email-success:
    runs-on: ubuntu-latest
    needs: trello-deployed
    if: success()
    name: 📧 Notify Team
    steps:
      - name: 📨 Send Success Notification
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │              🎉 PIPELINE COMPLETED SUCCESSFULLY                 │
          ├─────────────────────────────────────────────────────────────────┤
          │  Sending notification to team...                                │
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

      - name: 📧 Email Team
        continue-on-error: true
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "✅ Deployment Complete: ${{ github.event.inputs.card_title }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: DevOps Pipeline <noreply@github.com>
          body: |
            ╔════════════════════════════════════════════════════════════╗
            ║   🎉 DEPLOYMENT PIPELINE COMPLETED SUCCESSFULLY           ║
            ╚════════════════════════════════════════════════════════════╝
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📊 DEPLOYMENT DETAILS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            📝 Task Title      : ${{ github.event.inputs.card_title }}
            🎫 Card ID         : ${{ github.event.inputs.card_id }}
            📍 Commit Hash     : ${{ github.sha }}
            👤 Deployed By     : ${{ github.actor }}
            🌿 Branch          : ${{ github.ref }}
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🌐 LIVE ENVIRONMENT
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            📍 URL: https://gdgcloud-480509.web.app
            ✅ Status: DEPLOYED & LIVE
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📋 PIPELINE STAGES
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            ✅ Confirm Ready to Deploy
            ✅ Code Pushed
            ✅ Test & Build
            ✅ Deploy to Cloud
            ✅ Update Trello Status
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            🎊 Great job! The deployment is live!


  trello-failure:
    runs-on: ubuntu-latest
    if: failure()
    name: ⚠️ Sync Failure Status
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📊 Update Trello Failure
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │               ⚠️  PIPELINE FAILED - MARKING IN TRELLO            │
          ├─────────────────────────────────────────────────────────────────┤
          │  Status  : FAILURE
          │  Card ID : ${{ github.event.inputs.card_id }}
          │  Action  : Updating Trello board...
          └─────────────────────────────────────────────────────────────────┘
          
          EOF
        env:
          CARD_ID: ${{ github.event.inputs.card_id }}

      - name: 🔗 Execute Trello Update
        run: node .github/scripts/trello-update.js
        env:
          TRELLO_API_KEY: ${{ secrets.TRELLO_API_KEY }}
          TRELLO_TOKEN: ${{ secrets.TRELLO_TOKEN }}
          TRELLO_BOARD_ID: 694951faa7d6cc8841fe7bae
          TRELLO_TODO_LIST_ID: 694951faa7d6cc8841fe7be9
          TRELLO_INPROGRESS_LIST_ID: 694951faa7d6cc8841fe7bea
          TRELLO_READY_LIST_ID: 6949996bf592ed88a155869d
          TRELLO_DEPLOYED_LIST_ID: 694951faa7d6cc8841fe7beb
          BUILD_STATUS: failure
          CARD_ID: ${{ github.event.inputs.card_id }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_ACTOR: ${{ github.actor }}

  email-failure:
    runs-on: ubuntu-latest
    if: failure()
    name: 📧 Alert Team
    steps:
      - name: 📨 Send Failure Alert
        run: |
          cat << 'EOF'
          
          ┌─────────────────────────────────────────────────────────────────┐
          │            ⚠️  PIPELINE FAILED - NOTIFYING TEAM                  │
          ├─────────────────────────────────────────────────────────────────┤
          │  Sending alert to team...                                       │
          └─────────────────────────────────────────────────────────────────┘
          
          EOF

      - name: 📧 Email Alert
        continue-on-error: true
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "❌ Deployment Failed: ${{ github.event.inputs.card_title }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: DevOps Pipeline <noreply@github.com>
          body: |
            ╔════════════════════════════════════════════════════════════╗
            ║   ⚠️  DEPLOYMENT PIPELINE FAILED                          ║
            ╚════════════════════════════════════════════════════════════╝
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            🔍 FAILURE DETAILS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            📝 Task Title      : ${{ github.event.inputs.card_title }}
            🎫 Card ID         : ${{ github.event.inputs.card_id }}
            📍 Commit Hash     : ${{ github.sha }}
            👤 Triggered By    : ${{ github.actor }}
            🌿 Branch          : ${{ github.ref }}
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            📋 NEXT STEPS
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            1️⃣  Check the pipeline logs for details
            2️⃣  Review the error logs
            3️⃣  Fix the issues in your code
            4️⃣  Re-run the workflow when ready
            
            🔗 View Logs: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
            
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
            
            Need help? Check the Actions logs for detailed error messages.
